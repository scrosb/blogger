<h1> All tags </h1>

<ul id="tags">
  <% @tags.each do |tag| %>
      <li>
        <%= tag.name %>
        <%= link_to "delete", tag.name, data: {confirm: "Really delete the tag?"}, method: :delete %>
      </li>
  <% end %>
</ul>
